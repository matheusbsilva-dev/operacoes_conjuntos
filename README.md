
# Operações com Conjuntos em Python

Este projeto demonstra as principais operações com conjuntos (`set`) em Python, como união, interseção, diferença, diferença simétrica, subconjunto e superconjunto.

## Como executar

```bash
python operacoes_conjuntos.py
